# Frontend (Vite) - used by Netlify builds
# Set this to the full base API URL your frontend should call (include /api)
VITE_API_URL="https://your-backend.example.com/api"

# Optional: dev-time proxy target for Vite (not used in production builds)
# BACKEND_PROXY_TARGET="http://127.0.0.1:8000"

# Backend / Express: allowed origins for CORS (comma-separated)
# Include your Netlify app origin and any other allowed origins
ALLOWED_ORIGINS="https://statuesque-kangaroo-5f4b87.netlify.app,http://localhost:5175"

# Example PDF server API key (optional)
# PDF_API_KEY="supersecret"
